apiVersion: v1
kind: Service
metadata:
  name: blastradius-frontend
  labels:
    app: blastradius
    component: frontend
spec:
  type: NodePort
  selector:
    app: blastradius
    component: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30300
