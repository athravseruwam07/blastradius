# Template only. Copy to ../backend-secret.yaml (one directory up — must
# live directly in k8s/, not here, so `kubectl apply -f k8s/` applies it),
# fill in real values. ../backend-secret.yaml is gitignored; never commit
# real secret values. This file intentionally lives in k8s/examples/, not
# k8s/, so `kubectl apply -f k8s/` never applies the placeholder version.
#
#   cp examples/backend-secret.example.yaml backend-secret.yaml
#   # edit backend-secret.yaml
#   kubectl apply -f backend-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: blastradius-backend-secret
  labels:
    app: blastradius
type: Opaque
stringData:
  DB_PASSWORD: change-me
  API_KEY: change-me
