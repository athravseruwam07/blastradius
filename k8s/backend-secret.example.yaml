# Template only — copy to backend-secret.yaml, fill in real values, and
# apply that instead. backend-secret.yaml is gitignored; never commit real
# secret values.
#
#   cp backend-secret.example.yaml backend-secret.yaml
#   # edit backend-secret.yaml
#   kubectl apply -f backend-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: blastradius-backend-secret
  labels:
    app: blastradius
type: Opaque
stringData:
  DB_PASSWORD: change-me
  API_KEY: change-me
