apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blastradius-postgres-pvc
  labels:
    app: blastradius
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
