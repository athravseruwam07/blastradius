apiVersion: v1
kind: Service
metadata:
  name: blastradius-postgres
  labels:
    app: blastradius
    component: postgres
spec:
  selector:
    app: blastradius
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
